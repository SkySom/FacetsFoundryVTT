<section class="tab remoteUsers {{tab.cssClass}}" data-group="primary" data-tab="remoteUsers">
    <fieldset class="remote-users-container">
        <legend>{{localize tabs.remoteUsers.label}}</legend>
        {{#each users as |user|}}
            <div class="form-group">
                <label for={{user.id}}>{{user.name}}</label>
                <div class="form-fields">
                    <select id={{user.id}} name="remoteUser.{{user.id}}" {{#unless @root.remoteValid}}disabled{{/unless}}>
                        {{selectOptions @root.remoteUserOptions valueAttr="id" labelAttr="name" selected=user.remoteId}}
                    </select>
                </div>
            </div>
        {{/each}}
    </fieldset>
</section>